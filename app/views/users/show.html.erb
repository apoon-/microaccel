<h1><%= @user.username %></h1>
<%= @user.email %>

<h1>Your Projects</h1>
<% @userprojects.each do |p| %>
  <%= p.title %></br>
<% end %>

<h1>Your Pledges</h1>

<% @user.pledges.each do |p| %>
  <%= Project.find_by_id(p.id).title unless Project.find_by_id(p.id) == nil%>
<% end %>

